# Minimal CPU-only dependencies for snptx-repro-chembl.
# Pinned to versions used in the reference run; CPU torch wheels are on
# the standard PyPI index, so plain `pip install -r requirements.txt` works.
torch==2.5.1
torch_geometric==2.7.0
numpy>=1.26,<3
pandas>=2.0,<3
scikit-learn>=1.3,<2
pytest>=7
